\chapter[Appendix: Case Analysis Preliminaries]{Appendix: Case Analysis Preliminaries}
\label{app:case analysis Preliminaries}

In the following seven appendices, we will lay out the complete cases analyses on the candelabra train track and train tracks 0,1,8,12,13,17 to completely list the possible train track maps carried by these train tracks which can possibly lift to fixed-point free train track maps. To that end, we consider all possible images of the real edges of these train tracks. 

The primary obstruction utilized in these case analyses is the trace lemma, which states that the image of any real edge adjacent to a monogon cannot run over itself. For the Candelabra, this applies to all six real edges. For Tracks 0,1,8,12,13,17 of the stratum ???, this applies to all but the read edge $d$ which connects the two triangles. For this edge, the trace lemma applies in the following sense: there must be an even number of letters between any two occurrence of $d$ in $\fbd$. Similarly, 


\section{Rule F1}
\begin{rulee}
    frfrfrf
\end{rulee}